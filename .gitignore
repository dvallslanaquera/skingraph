.env
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.coverage
htmlcov/
.mypy_cache/
.ruff_cache/

# Generated by Docker/CI via `poetry export` — never commit
/requirements.txt

# Windows Explorer metadata
desktop.ini
Thumbs.db
data/golden_set/*.jpg
data/users.db
data/registry_candidates.json
data/qdrant/
# Opt-in captures of bounced frames (may contain PII) + web-search result cache
data/rejections/
data/web_cache.json
# Local labeled images for eval/vision_eval.py (manifest is committed, images aren't)
data/vision_eval/
.DS_Store
.TODO.md

# Claude Code local state (plan files, harness scratch)
.claude/

# Terraform — contains secrets (google_api_key)
terraform/terraform.tfvars
terraform/.terraform/
terraform/.terraform.lock.hcl
terraform/terraform.tfstate
terraform/terraform.tfstate.backup
.vercel
